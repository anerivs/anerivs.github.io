<!--  we put global CSS here because @apply does not get parsed in .css files -->
<style is:global>
  @tailwind base;
  @tailwind components;
  @tailwind utilities;

  body {
    font-family: "PP Mori", sans-serif;
    line-height: calc(1.2em + 1ex);
    @apply bg-b-white
      text-b-willow-dark text-lg;
  }

  :root {
    --gutter: 2rem;
  }

  @media screen and (min-width: 1024px) {
    :root {
      --gutter: 4rem;
    }
  }

  @media screen and (min-width: 1280px) {
    :root {
      --gutter: 6rem;
    }
  }

  @media screen and (min-width: 1536px) {
    :root {
      --gutter: 12rem;
    }
  }

  @font-face {
    font-family: "PP Mori";
    src:
      url("/fonts/PPMori-ExtraBold.woff2") format("woff2"),
      url("/fonts/PPMori-ExtraBold.woff") format("woff");
    font-weight: bold;
    font-style: normal;
    font-display: swap;
  }

  @font-face {
    font-family: "PP Mori";
    src:
      url("/fonts/PPMori-ExtraBoldItalic.woff2") format("woff2"),
      url("/fonts/PPMori-ExtraBoldItalic.woff") format("woff");
    font-weight: bold;
    font-style: italic;
    font-display: swap;
  }

  @font-face {
    font-family: "PP Mori";
    src:
      url("/fonts/PPMori-SemiBoldItalic.woff2") format("woff2"),
      url("/fonts/PPMori-SemiBoldItalic.woff") format("woff");
    font-weight: 600;
    font-style: italic;
    font-display: swap;
  }

  @font-face {
    font-family: "PP Mori";
    src:
      url("/fonts/PPMori-Regular.woff2") format("woff2"),
      url("/fonts/PPMori-Regular.woff") format("woff");
    font-weight: normal;
    font-style: normal;
    font-display: swap;
  }

  @font-face {
    font-family: "PP Mori";
    src:
      url("/fonts/PPMori-Extralight.woff2") format("woff2"),
      url("/fonts/PPMori-Extralight.woff") format("woff");
    font-weight: 100;
    font-style: normal;
    font-display: swap;
  }

  @font-face {
    font-family: "PP Mori";
    src:
      url("/fonts/PPMori-RegularItalic.woff2") format("woff2"),
      url("/fonts/PPMori-RegularItalic.woff") format("woff");
    font-weight: normal;
    font-style: italic;
    font-display: swap;
  }

  @font-face {
    font-family: "PP Mori";
    src:
      url("/fonts/PPMori-SemiBold.woff2") format("woff2"),
      url("/fonts/PPMori-SemiBold.woff") format("woff");
    font-weight: 600;
    font-style: normal;
    font-display: swap;
  }

  @font-face {
    font-family: "PP Mori Book";
    src:
      url("/fonts/PPMori-BookItalic.woff2") format("woff2"),
      url("/fonts/PPMori-BookItalic.woff") format("woff");
    font-weight: 300;
    font-style: italic;
    font-display: swap;
  }

  @font-face {
    font-family: "PP Mori";
    src:
      url("/fonts/PPMori-ExtralightItalic.woff2") format("woff2"),
      url("/fonts/PPMori-ExtralightItalic.woff") format("woff");
    font-weight: 100;
    font-style: italic;
    font-display: swap;
  }

  @font-face {
    font-family: "PP Mori Book";
    src:
      url("/fonts/PPMori-Book.woff2") format("woff2"),
      url("/fonts/PPMori-Book.woff") format("woff");
    font-weight: 300;
    font-style: normal;
    font-display: swap;
  }

  .content-md {
    @apply text-b-willow-dark;
  }

  .content-md > :first-child {
    @apply -mt-2;
  }

  .content-md > :first-child:is(h1, h2, h3, h4, h5, h6) {
    @apply leading-[0.95] mt-0;
  }

  .content-md h1 {
    @apply text-3xl mb-[0.75ex] mt-[1.25em] text-b-gray;
  }

  .content-md h2 {
    @apply text-2xl mb-[0.75ex] mt-[1.25em] text-b-gray;
  }

  .content-md h3 {
    @apply text-xl mb-[0.75ex] mt-[1.25em] text-b-gray;
  }

  .content-md h4 {
    @apply text-lg mb-[0.75ex] mt-[1.25em] text-b-gray;
  }

  .content-md h5 {
    @apply text-base mb-[0.75ex] mt-[1.25em] text-b-gray;
  }

  .content-md p {
    @apply mb-[1ex] mt-[1.125ex];
  }

  .content-md a {
    @apply underline underline-offset-1 decoration-wavy decoration-b-minato text-b-minato;
  }

  .content-md pre {
    @apply p-4 border-b-willow border rounded-sm mb-6 text-base;
  }

  .content-md img {
    @apply mb-6;
  }

  .content-md ul {
    @apply list-disc list-inside;
  }

  .content-md ol {
    @apply list-decimal list-inside;
  }

  .noise {
    position: absolute;
    inset: 0;
    z-index: 0;
    mix-blend-mode: overlay;
    background-image: url(/noise.webp);
    pointer-events: none;
  }
  .contact-noise {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    z-index: 1;
    mix-blend-mode: overlay;
    background-image: url(/noise.webp);
    pointer-events: none;
}
.intro-noise {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    z-index: 1; /* Make sure it's above the background but below the text */
    mix-blend-mode: overlay;
    background-image: url(/noise.webp);
    pointer-events: none; /* Ensure it doesn't interfere with user interaction */
}

.header-noise {
    position: absolute;
    inset: 0;
    z-index: 1;
    mix-blend-mode: overlay;
    background-image: url(/noise.webp);
    pointer-events: none;
    border-radius: 50%; /* Ensure the noise fits within the dot */
}


  .green-fade {
    background: linear-gradient(180deg, #125178 0%, #445322 100%);
  }
  
  .image-container {
  position: relative;
  width: 250px;
  height: 325px;
  overflow: hidden;
}

.aneri-img {
  width: 100%;
  height: 100%;
  display: block;
  box-shadow: 10px 10px 30px rgba(0, 0, 0, 0.3); /* Increased shadow size and made it a soft, dark shadow */
  object-fit: cover; /* Ensures the image covers the container without distortion */
  z-index: 1; /* Ensures the image is above other elements if necessary */
}


.frame-overlay {
  position: absolute;
  top: -175px;
  left: 40px;
  width: 750px;
  height: 750px;
  object-fit: cover;
}

</style>
